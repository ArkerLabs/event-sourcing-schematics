{
  "$schema": "http://json-schema.org/schema",
  "id": "SchematicsCommand",
  "type": "object",
  "properties": {
    "name": {
      "type": "string",
      "description": "The name of the command.",
      "$default": {
        "$source": "argv",
        "index": 0
      },
      "x-prompt": "What name would you like to use for the command?"
    },
    "domain": {
      "type": "string",
      "description": "The domain in which to create the command.",
      "x-prompt": "What domain would you like to use for the command?"
    },
    "path": {
      "type": "string",
      "format": "path",
      "description": "The path to create the command."
    },
    "language": {
      "type": "string",
      "description": "Nest command language (ts/js)."
    },
    "sourceRoot": {
      "type": "string",
      "description": "Nest command source root directory."
    },
    "skipImport": {
      "description": "Flag to skip the module import.",
      "default": false
    },
    "module": {
      "type": "string",
      "description": "Allows specification of the declaring module."
    },
    "flat": {
      "default": false,
      "description": "Flag to indicate if a directory is created."
    },
    "spec": {
      "default": true,
      "description": "Specifies if a spec file is generated."
    }
  },
  "required": ["name"]
}
